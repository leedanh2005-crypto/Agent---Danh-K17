Powersheel: python -m streamlit run app.py

Kiểm tra mã API: python -c "import google.generativeai as genai; genai.configure(api_key='YOUR_API_KEY_HERE'); [print(m.name) for m in genai.list_models()]"


import streamlit as st
import google.generativeai as genai
import os

# 1. Cấu hình AI
# KHÔNG dán mã API thật vào đây để tránh bị khóa.
# Hãy sử dụng hệ thống Secrets của Streamlit.
genai.configure(api_key="YOUR_API_KEY_HERE")
model = genai.GenerativeModel('gemini-flash-latest')

# 2. Giao diện Web
st.set_page_config(page_title="HR Student Assistant", page_icon="🤝", layout="wide")

# ... rest of file (CSS, Hero Section, etc. remains the same) ...
